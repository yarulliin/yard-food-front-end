import { ConvertFromIsoPipe } from './convert-from-iso.pipe';

describe('ConvertFromIsoPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertFromIsoPipe();
    expect(pipe).toBeTruthy();
  });
});
